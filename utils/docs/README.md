# docs

Generates the documentation site:

1. Installs Python dependencies (PyYAML)
2. Runs `parse_actions.py` to generate reference pages from `action.yml` files
3. Builds the Antora documentation site

## Usage

```bash
npm run docs
```
