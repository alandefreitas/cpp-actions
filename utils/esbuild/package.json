{
  "name": "esbuild-cli",
  "version": "1.9.3",
  "private": true,
  "description": "esbuild bundler for GitHub Actions with proper source maps",
  "main": "dist/index.js",
  "bin": {
    "esbuild-cli": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "esbuild": "^0.27.3"
  }
}
