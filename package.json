{
  "name": "cpp-actions",
  "version": "1.9.2",
  "private": true,
  "description": "GitHub Actions for C++ projects",
  "workspaces": [
    "common/gh-inputs",
    "common/pretty-errors",
    "common/trace-commands",
    "utils/esbuild",
    "utils/jsdoc-linter",
    "utils/build",
    "utils/release",
    "b2-workflow",
    "boost-clone",
    "cmake-workflow",
    "cpp-matrix",
    "create-changelog",
    "flamegraph",
    "package-install",
    "setup-clang",
    "setup-cmake",
    "setup-cpp",
    "setup-gcc",
    "setup-msvc",
    "setup-program"
  ],
  "scripts": {
    "build": "node utils/build/dist/index.js",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "prepare": "npm run prepare:foundation && npm run prepare:actions",
    "prepare:foundation": "npm run build -w utils/esbuild && npm run all -w common/trace-commands && npm run all -w common/gh-inputs && npm run all -w common/pretty-errors && npm run build -w utils/jsdoc-linter && npm run build -w utils/build && npm run build -w utils/release",
    "prepare:actions": "npm run all -w setup-program && npm run all -w setup-msvc && npm run all -w b2-workflow && npm run all -w flamegraph && npm run all -w create-changelog && npm run all -w setup-clang && npm run all -w setup-cmake && npm run all -w setup-gcc && npm run all -w package-install && npm run all -w boost-clone && npm run all -w cpp-matrix && npm run all -w cmake-workflow && npm run all -w setup-cpp",
    "lint:jsdoc": "node utils/jsdoc-linter/dist/index.js",
    "depcheck": "npm exec --workspaces -- depcheck",
    "update-deps": "npm exec --workspaces -- npm-check-updates -u && npm install",
    "version:set": "npm version --workspaces --include-workspace-root --no-git-tag-version --allow-same-version",
    "docs": "node utils/build/dist/index.js --docs",
    "release": "node utils/release/dist/index.js"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.0",
    "typescript": "^5.6.0"
  }
}
