{
  "name": "pretty-errors",
  "version": "1.0.0",
  "description": "Small helper to render source-mapped, Youch-formatted errors for GitHub Actions",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "tsc -p .config/tsconfig.json",
    "prepare": "npm run build && ncc build lib/index.js -o dist --source-map --license licenses.txt --minify",
    "typecheck": "tsc -p .config/tsconfig.json --noEmit",
    "test": "jest --config ../../jest.config.js --selectProjects pretty-errors",
    "all": "npm run prepare && npm run test"
  },
  "keywords": [],
  "author": "",
  "license": "Boost Software License 1.0",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "stacktracey": "^2.1.8",
    "youch-terminal": "^2.2.3"
  }
}
