{
  "name": "package-install",
  "version": "1.9.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p .config/tsconfig.json",
    "prepare": "npm run build && ncc build lib/index.js -o dist --source-map --license licenses.txt --minify",
    "typecheck": "tsc -p .config/tsconfig.json --noEmit",
    "test": "jest --config ../jest.config.js --selectProjects package-install",
    "all": "npm run prepare && npm run test"
  },
  "keywords": [],
  "author": "",
  "license": "Boost Software License 1.0",
  "dependencies": {
    "@actions/cache": "^4.1.0",
    "@actions/core": "^1.11.1",
    "@actions/exec": "^1.1.1",
    "@actions/io": "^2.0.0",
    "@actions/tool-cache": "^2.0.2",
    "semver": "^7.7.3",
    "trace-commands": "*",
    "gh-inputs": "*",
    "setup-program": "*",
    "pretty-errors": "*"
  },
  "devDependencies": {
    "@types/semver": "^7.5.0"
  }
}
